<Window x:Class="Employee_And_Company_Management.Views.Windows.Employees.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{DynamicResource EmployeePanel}" Height="600" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        TextElement.FontFamily="{materialDesign:MaterialDesignFont}">

    <materialDesign:Card >

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <Border Grid.Column="0" Style="{StaticResource MenuStyle}">
                <Grid VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <!-- This row will take all the remaining space -->
                        <RowDefinition Height="Auto"/>
                        <!-- This row will only take the height of the settings button -->
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0">
                        <Button Content="{DynamicResource Employments}" Style="{StaticResource ButtonStyle}" Margin="10" Command="{Binding NavigateToEmploymentsCommand}"/>
                    </StackPanel>

                    <Button Grid.Row="1" Content="{DynamicResource Settings}" Style="{StaticResource ButtonStyle}" Margin="10" Command="{Binding NavigateToSettingsCommand}"/>
                </Grid>
            </Border>
        

            <!-- Right Panel -->
            <Border Grid.Column="1" Style="{StaticResource RightPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Main Content Area -->
                    <ContentControl x:Name="MainContentControl" Grid.Row="0" Margin="20" Content="{Binding CurrentPage}"/>
                </Grid>
            </Border>
        </Grid>
    </materialDesign:Card>
</Window>
